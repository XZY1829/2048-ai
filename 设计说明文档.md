# C++ 2048 游戏 + AI 冲分助手 — 设计说明文档

## 1. 项目概述

### 1.1 目标

用 C++ 从零实现一个终端版 2048 游戏，并开发配套的 AI 冲分助手，能够自动决策达到尽可能高的分数（目标：稳定到达 2048，冲击 4096/8192/16384+）。

### 1.2 技术选型

| 维度 | 选择 | 理由 |
|------|------|------|
| 语言标准 | C++17 | 结构化绑定、`std::optional`、`if constexpr` 方便开发 |
| 构建系统 | CMake 3.20+ | 跨平台，支持 CTest |
| 平台 | Windows 控制台（主），可选 Linux/macOS |
| UI 方式 | 终端渲染（Windows Console API / ANSI escape） |
| 依赖 | **零外部依赖**，纯标准库 + 系统 API |

### 1.3 交付物

1. 可玩的终端 2048 游戏（人类玩家模式）
2. AI 自动冲分模式（观看 AI 实时下棋）
3. AI 提示模式（人类操控，AI 建议最优方向）
4. 性能基准报告（AI 每秒决策数、平均分数统计）

---

## 2. 游戏核心逻辑

### 2.1 棋盘表示

采用 **64 位整数 bitboard**，每个 tile 用 4 bit 存储（存的是 log2 值，0 表示空，1 表示 2，2 表示 4 …… 15 表示 32768）。

```
Board Layout (4x4 grid → 64-bit uint64_t):

 ┌────┬────┬────┬────┐
 │ 60 │ 56 │ 52 │ 48 │  ← row 0 (bits 48-63)
 ├────┼────┼────┼────┤
 │ 44 │ 40 │ 36 │ 32 │  ← row 1 (bits 32-47)
 ├────┼────┼────┼────┤
 │ 28 │ 24 │ 20 │ 16 │  ← row 2 (bits 16-31)
 ├────┼────┼────┼────┤
 │ 12 │  8 │  4 │  0 │  ← row 3 (bits 0-15)
 └────┴────┴────┴────┘
```

**优势**：
- 单次 move 操作只需查表（65536 项预计算表），O(1) 完成
- 整个棋盘状态可作为 hash map 的 key（transposition table）
- 极致的内存效率，cache-friendly

### 2.2 移动逻辑

预计算一张 `uint16_t → uint16_t` 的行变换表（65536 项），对应一行向左/向右滑动后的结果。列操作通过转置实现。

```cpp
// 伪代码
uint16_t row_left_table[65536];   // row → moved_row
uint16_t row_right_table[65536];  // row → moved_row
float    heur_score_table[65536]; // row → heuristic contribution

void init_tables() {
    for (int row = 0; row < 65536; row++) {
        // 解码 4 个 nybble
        int a = (row >> 12) & 0xF;
        int b = (row >>  8) & 0xF;
        int c = (row >>  4) & 0xF;
        int d = (row >>  0) & 0xF;
        // 模拟滑动合并 ...
    }
}
```

### 2.3 新方块生成

- 90% 概率生成 2（log2 值 = 1）
- 10% 概率生成 4（log2 值 = 2）
- 随机选择一个空位放置

### 2.4 游戏结束判定

四个方向都无法产生有效移动时 Game Over。用 bitboard 可高效检测：对每个方向尝试 move，若结果都等于原始 board 则结束。

---

## 3. 终端渲染

### 3.1 渲染方案

| 方案 | 平台 | 特点 |
|------|------|------|
| Windows Console API | Windows | `SetConsoleCursorPosition` + `SetConsoleTextAttribute`，无闪烁 |
| ANSI Escape Codes | 跨平台 | `\033[H` 光标归位 + `\033[38;5;Xm` 256 色 |
| 双缓冲 | 通用 | 先写入 buffer 再一次性输出，避免撕裂 |

推荐：优先使用 ANSI escape（Windows 10+ 已支持），fallback 到 Console API。

### 3.2 UI 布局

```
╔══════════════════════════════════╗
║        2 0 4 8  Game             ║
╠══════════════════════════════════╣
║  Score: 12480    Best: 28672     ║
║  Moves: 342     AI: ON          ║
╠══════════════════════════════════╣
║ ┌──────┬──────┬──────┬──────┐   ║
║ │  512 │  256 │   64 │    4 │   ║
║ ├──────┼──────┼──────┼──────┤   ║
║ │  128 │   32 │   16 │    2 │   ║
║ ├──────┼──────┼──────┼──────┤   ║
║ │    8 │    4 │    2 │      │   ║
║ ├──────┼──────┼──────┼──────┤   ║
║ │    2 │      │      │      │   ║
║ └──────┴──────┴──────┴──────┘   ║
╠══════════════════════════════════╣
║  [←↑↓→] Move  [A] AI  [Q] Quit ║
╚══════════════════════════════════╝
```

### 3.3 配色方案

每种 tile 值对应不同的前景/背景颜色，参照经典 2048 配色：

| Tile | 背景色 | 文字色 |
|------|--------|--------|
| 2    | #eee4da | #776e65 |
| 4    | #ede0c8 | #776e65 |
| 8    | #f2b179 | #f9f6f2 |
| 16   | #f59563 | #f9f6f2 |
| 32   | #f67c5f | #f9f6f2 |
| 64   | #f65e3b | #f9f6f2 |
| 128  | #edcf72 | #f9f6f2 |
| 256  | #edcc61 | #f9f6f2 |
| 512  | #edc850 | #f9f6f2 |
| 1024 | #edc53f | #f9f6f2 |
| 2048 | #edc22e | #f9f6f2 |
| 4096+| #3c3a32 | #f9f6f2 |

终端下用最近似的 256 色 / 24-bit true color ANSI 代码映射。

---

## 4. AI 冲分助手设计

### 4.1 算法选型对比

| 算法 | 胜率(2048) | 最高 tile | 每步耗时 | 实现复杂度 | 推荐 |
|------|-----------|-----------|---------|-----------|------|
| **Expectimax + Heuristic** | ~95% | 32768 | 10-100ms | ★★★ | **主选** |
| Monte Carlo Tree Search | ~84% | 16384 | 200ms+ | ★★ | 备选 |
| Minimax + α-β 剪枝 | ~70% | 8192 | 50ms | ★★ | 不推荐（假设对手最恶） |
| 纯贪心/规则 | ~40% | 4096 | <1ms | ★ | 作为 baseline |
| TD-Learning (N-tuple) | ~97% | 65536 | <1ms/步 | ★★★★ | 进阶版 |

**决策：采用 Expectimax 作为主力算法**，配合 bitboard + lookup table 优化达到高性能。后续可选加入 TD-Learning 做对比。

### 4.2 Expectimax 算法原理

```
Expectimax(state, depth):
    if depth == 0 or game_over(state):
        return heuristic_eval(state)
    
    if is_player_turn:                    // MAX 节点
        best = -∞
        for move in [UP, DOWN, LEFT, RIGHT]:
            new_state = apply_move(state, move)
            if new_state != state:        // 有效移动
                best = max(best, Expectimax(new_state, depth-1))
        return best
    
    else:                                 // CHANCE 节点（随机放砖）
        expected = 0
        empty_cells = count_empty(state)
        for each empty cell:
            expected += 0.9 * Expectimax(place(state, cell, 2), depth-1)
            expected += 0.1 * Expectimax(place(state, cell, 4), depth-1)
        return expected / empty_cells
```

### 4.3 启发式评估函数

评估函数是 AI 性能的关键。采用**多特征加权求和**：

```
Score = w1 * monotonicity + w2 * smoothness + w3 * empty_tiles + w4 * max_tile_corner + w5 * merge_potential
```

#### 4.3.1 单调性（Monotonicity）

衡量每行/每列是否按递增或递减排列。单调的棋盘更容易连续合并。

```cpp
float monotonicity(uint64_t board) {
    float score = 0;
    for (int i = 0; i < 4; i++) {
        uint16_t row = get_row(board, i);
        // 检查行是否递减或递增，给予奖励
        // 违反单调性则按差值惩罚
    }
    // 对列做同样处理（转置后提取行）
    return score;
}
```

#### 4.3.2 平滑度（Smoothness）

相邻 tile 值的差越小越好，有利于合并。

```cpp
float smoothness(uint64_t board) {
    float penalty = 0;
    for (each pair of adjacent non-empty tiles) {
        penalty -= abs(log2(tile_a) - log2(tile_b));
    }
    return penalty;
}
```

#### 4.3.3 空格数（Empty Tiles）

空格越多，灵活性越大，离 Game Over 越远。

#### 4.3.4 角落策略（Corner Strategy）

最大 tile 位于角落给予大幅奖励。使用蛇形权重矩阵：

```
权重矩阵（Snake Pattern）:
┌────────┬────────┬────────┬────────┐
│  15    │  14    │  13    │  12    │
├────────┼────────┼────────┼────────┤
│   8    │   9    │  10    │  11    │
├────────┼────────┼────────┼────────┤
│   7    │   6    │   5    │   4    │
├────────┼────────┼────────┼────────┤
│   0    │   1    │   2    │   3    │
└────────┴────────┴────────┴────────┘
```

#### 4.3.5 合并潜力（Merge Potential）

统计相邻相同 tile 的对数，越多越好。

### 4.4 性能优化技术

#### 4.4.1 Lookup Table（查表法）

- **行变换表**（65536 项）：给定一行 16-bit 编码，直接查出移动后的结果
- **评估分数表**（65536 项）：给定一行，直接查出该行的启发式贡献值
- 整个 board 的评估 = 4 行 + 4 列的评估之和（6 次查表）

#### 4.4.2 Transposition Table（置换表）

用 `unordered_map<uint64_t, CacheEntry>` 存储已评估过的局面，避免重复计算。

```cpp
struct CacheEntry {
    uint8_t depth;      // 该条目评估时的搜索深度
    float   heuristic;  // 缓存的评估值
};
```

#### 4.4.3 概率裁剪（Probability Pruning）

当一条路径的累积概率低于阈值时，直接剪枝不再展开：

```cpp
if (cumulative_probability < CPROB_THRESH) {
    return heuristic_eval(board);  // 不再深搜
}
```

典型阈值：`0.0001`（路径需经过多次 10% 的 4-tile 才会被剪掉）。

#### 4.4.4 迭代加深（Iterative Deepening）

- 初始搜索深度 3
- 若时间允许，逐步加深到 4、5、6...
- 空格多时浅搜（分支因子大），空格少时深搜（关键时刻）

---

## 5. 模块架构

```
2048-cpp/
├── CMakeLists.txt
├── README.md
├── include/
│   ├── board.h            // Board 类：bitboard 表示 + 移动操作
│   ├── game.h             // Game 逻辑：分数、状态管理、游戏循环
│   ├── renderer.h         // 终端渲染接口
│   ├── input.h            // 输入处理（键盘事件）
│   ├── ai/
│   │   ├── evaluator.h    // 启发式评估函数
│   │   ├── expectimax.h   // Expectimax 搜索引擎
│   │   ├── ai_player.h   // AI 玩家封装（决策接口）
│   │   └── lookup.h       // 预计算查找表
│   └── utils.h            // 计时器、随机数等工具
├── src/
│   ├── main.cpp           // 入口：模式选择
│   ├── board.cpp
│   ├── game.cpp
│   ├── renderer.cpp
│   ├── input.cpp
│   ├── ai/
│   │   ├── evaluator.cpp
│   │   ├── expectimax.cpp
│   │   ├── ai_player.cpp
│   │   └── lookup.cpp
│   └── utils.cpp
├── tests/
│   ├── test_board.cpp     // 棋盘操作单元测试
│   ├── test_move.cpp      // 移动逻辑正确性测试
│   ├── test_ai.cpp        // AI 决策质量测试
│   └── CMakeLists.txt
└── benchmark/
    └── bench_ai.cpp       // AI 性能基准测试
```

### 5.1 关键类设计

```cpp
// ===== Board =====
class Board {
public:
    using board_t = uint64_t;
    
    Board();
    Board(board_t raw);
    
    // 移动操作（返回新 board + 本次合并得分）
    std::pair<Board, int> move_left() const;
    std::pair<Board, int> move_right() const;
    std::pair<Board, int> move_up() const;
    std::pair<Board, int> move_down() const;
    
    // 查询
    int  get_tile(int row, int col) const;  // 返回实际值(2,4,8...)
    int  count_empty() const;
    int  max_tile() const;
    bool is_game_over() const;
    
    // 随机放砖
    Board spawn_tile(std::mt19937& rng) const;
    
    board_t raw() const { return raw_; }
    
private:
    board_t raw_;
    static uint16_t move_left_table_[65536];
    static uint16_t move_right_table_[65536];
    static float    score_table_[65536];
    static bool     tables_initialized_;
    static void     init_tables();
};

// ===== AI Player =====
class AIPlayer {
public:
    struct Config {
        int    max_depth = 6;       // 最大搜索深度
        float  prob_threshold = 0.0001f;
        int    time_limit_ms = 100; // 单步时间限制
    };
    
    AIPlayer(Config config = {});
    
    // 返回最佳方向 (0=UP, 1=DOWN, 2=LEFT, 3=RIGHT)
    int best_move(const Board& board);
    
    // 统计信息
    struct Stats {
        int   nodes_evaluated;
        int   cache_hits;
        int   max_depth_reached;
        float elapsed_ms;
    };
    Stats last_stats() const;
    
private:
    Config config_;
    std::unordered_map<uint64_t, CacheEntry> trans_table_;
    
    float expectimax_move(const Board& board, int depth, float cprob);
    float expectimax_chance(const Board& board, int depth, float cprob);
};
```

---

## 6. 游戏模式

### 6.1 模式一：人类玩家

- 方向键 / WASD 控制移动
- 实时渲染棋盘
- 显示分数、步数

### 6.2 模式二：AI 自动冲分

- AI 全自动操作
- 可调速（延迟 0ms ~ 500ms/步，便于观看）
- 显示 AI 决策的搜索深度、评估节点数、耗时

### 6.3 模式三：AI 提示

- 人类操控，AI 在旁实时显示推荐方向
- 类似"辅助驾驶"模式

### 6.4 模式四：性能基准测试

- 连续运行 N 局（默认 100 局）
- 统计：平均分、最高分、各 tile 达成率、平均 moves/sec
- 输出结果到终端 + 可选 CSV

---

## 7. 开发路线图

### Phase 1：游戏核心（预计 2-3 天）

- [ ] 实现 bitboard 棋盘表示
- [ ] 预计算 move/score lookup table
- [ ] 实现四方向移动、合并、得分计算
- [ ] 新方块随机生成
- [ ] Game Over 检测
- [ ] 单元测试覆盖

### Phase 2：终端渲染与交互（预计 1-2 天）

- [ ] ANSI 终端渲染器
- [ ] 颜色映射
- [ ] 键盘输入处理（Windows `_getch()` / Linux `termios`）
- [ ] 人类可玩模式完成

### Phase 3：AI Expectimax 引擎（预计 3-4 天）

- [ ] 启发式评估函数实现
- [ ] Expectimax 递归搜索
- [ ] Transposition Table
- [ ] 概率裁剪
- [ ] 迭代加深
- [ ] 权重调优（手动 + 可选 CMA-ES 自动调参）
- [ ] AI 自动/提示模式接入

### Phase 4：优化与打磨（预计 2-3 天）

- [ ] 性能 profiling 和优化
- [ ] 基准测试框架
- [ ] 参数可配置（depth, time limit 等通过命令行/菜单调整）
- [ ] 存档/读档功能
- [ ] 排行榜

### Phase 5（可选进阶）：TD-Learning

- [ ] N-tuple Network 价值函数
- [ ] 自我博弈训练
- [ ] 对比 Expectimax 与 TD-Learning 效果

---

## 8. 预期性能指标

| 指标 | 目标值 | 参考（nneonneo/2048-ai） |
|------|--------|--------------------------|
| 搜索速度 | >1M nodes/sec | ~10M nodes/sec |
| 单步决策 | <100ms (depth 5-6) | ~50ms |
| 2048 tile 达成率 | >90% | ~95% |
| 4096 tile 达成率 | >60% | ~70% |
| 8192 tile 达成率 | >20% | ~30% |
| 平均分（100 局） | >30,000 | ~40,000 |

---

## 9. 参考资料

1. **nneonneo/2048-ai** — 业界标杆 Expectimax C++ 实现，bitboard + lookup table  
   https://github.com/nneonneo/2048-ai

2. **StackOverflow: What is the optimal algorithm for 2048?** — 详细的算法分析与讨论  
   https://stackoverflow.com/questions/22342854/what-is-the-optimal-algorithm-for-the-game-2048

3. **Robert Xiao: Writing a 2048 AI** — Expectimax 详细教程  
   https://www.robertxiao.ca/hacking/2048-ai/

4. **moporgic/TDL2048** — TD-Learning + N-tuple 的极致优化实现  
   https://github.com/moporgic/TDL2048

5. **Stanford CS229: AI Plays 2048** — 多种启发式与权重矩阵对比  
   https://cs229.stanford.edu/proj2016/report/NieHouAn-AIPlays2048-report.pdf

6. **ziap/2048-ai** — Zig/C++ Expectimax + bitboard + WebAssembly 并行  
   https://github.com/ziap/2048-ai

7. **arXiv: Merge and Conquer** — 进化算法优化 2048 启发式权重  
   https://arxiv.org/html/2510.20205v1

---

## 10. 快速启动（开发后）

```bash
# 构建
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release

# 人类模式
./2048 --mode play

# AI 自动冲分
./2048 --mode ai --speed 50

# AI 提示模式
./2048 --mode hint

# 基准测试（100 局）
./2048 --mode bench --games 100
```

---

## 11. 总结

本项目旨在通过纯 C++ 实现一个高质量的 2048 游戏 + AI 系统，核心技术点包括：

- **Bitboard 表示**：64-bit 编码整个 4×4 棋盘，查表法 O(1) 执行移动
- **Expectimax 搜索**：处理随机性的最优博弈树搜索
- **多维启发式**：单调性 + 平滑度 + 空格数 + 角落策略 + 合并潜力
- **高性能优化**：lookup table + transposition table + 概率裁剪 + 迭代加深

项目既是 C++ 工程能力的锻炼（位运算、模板、性能优化），也是 AI/搜索算法的实战应用，适合作为个人项目或面试谈资。
